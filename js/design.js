//model: all the locations
var place = {};

//Octopus:
//1.init map, marker, info and animation 
//2.search filter the place, only search items on the page
//3. click animate the current marker
var control = {
	//init map, marker info animation
	init: function(){
		map.init();
		list.init();
	}
	//filter

};

//view: 
//1.render the map(left) and 
//2.render the list of places(right)
var map = {
	init: function(){
		//init map
		//init marker
		//add animation
		//show info
	}
	//current place display
	filter: function(){

	}
};

var list = {
	init: function(){
		
	}
};